#pragma once


enum class MAIN_MENU {
	VOID,
	SYMBOL,
	STRING,
	EXIT
};


enum class SYMBOL_MENU {
	VOID,
	ENCODE,
	ENCODE_2,
	DECODE,
	DECODE_2,
	EXIT
};


enum class STRING_MENU {
	VOID,
	ENCODE,
	ENCODE_2,
	DECODE,
	DECODE_2,
	EXIT
};